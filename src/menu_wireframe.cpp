#include "menu_wireframe.h"

// Implementation file intentionally left minimal — header-only UI lives in the header.
// This translation unit ensures the file is compiled and linked when listed in CMake.

// No additional code required.