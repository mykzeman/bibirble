#include "loading_dialog.h"

// Implementation file intentionally left minimal — dialog is implemented in the header.
// This ensures a compiled translation unit exists when CMake lists it as a source.